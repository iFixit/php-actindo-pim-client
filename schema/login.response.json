{
   "title": "Login response",
   "description": "Response received on successful login.",
   "type": "object",
   "properties": {
      "sessionId": {
         "description": "Authentication token",
         "type": "string",
         "minLength": 1
      }
   },
   "required": ["sessionId"]
}
